{
  "name": "valentinesproject",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix server/client run dev\"",
    "build": "npm --prefix server/client run build",
    "start": "node server/src/server.js",
    "install:all": "npm --prefix server install && npm --prefix server/client install"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}